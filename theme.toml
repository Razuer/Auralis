# Theme configuration (colors + UI)
# Managed by scripts/apply_theme.py

[accent]
primary = "#7dd6f6"
primary_bright = "#b3ecff"
secondary = "#7dd6f6"

[ui]
# Window rounded corners (0 = square, 10 = rounded)
rounding = 10  # config:rounding

# Waybar style
waybar_floating = true  # config:waybar_floating
